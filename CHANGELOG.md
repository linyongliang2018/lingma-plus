# 更新日志

## [1.3.0] - 2026-03-04

### 新增

- **自定义页容量**：配置中新增「页容量」设置（1-500），JavaClassScanner 分页支持每页 20 等自定义条数，持久化到本地配置

### 改进

- **扫描未提交修改**：原「扫描贮存区修改文件」改为「扫描未提交修改」，同时包含暂存区（已 add）与工作区（已修改未 add、IDE 蓝色）的 Java 文件，未加入 Git 管理的仍忽略
- **按类名排序**：扫描结果按完整类名排序后再分页展示，顺序稳定易查找
- **配置项**：LingmaHelper 配置对话框增加「JavaClassScanner 分页」-「页容量」输入框，底部提示展示当前页容量

---

## [1.2.0] - 2026-03-04

### 新增

- **开始注解**：在 JavaClassScanner 面板新增「开始注解」按钮，多选类后批量调用灵码注释生成功能（TriggerCosyCodeGenerateCommentGenerationAction）
- **多种批量策略复用**：
  - 带 `@Data` 的类：整类一次发送给灵码
  - 普通类：按方法级拆分逐个发送
  - 既没有 `@Data` 又没有任何方法的类会自动忽略
- **解释选中项**：表格右键菜单新增「对选中项进行解释」，按同样的类/方法拆分策略调用解释类 Action（TriggerCosyExplainCodeGenerationAction 或你的实际 Action Id）
- **扫描贮存区修改文件**：新增「扫描贮存区修改文件」按钮，使用 `git diff --cached --name-only -- *.java` 扫描当前项目中已加入 Git 管理且已暂存的 Java 文件（忽略未加入 Git 的文件）

### 改进

- **统一时间间隔配置**：优化、注释、解释三种批量调用统一复用 Lingma 配置中的最小/最大时间间隔，保持行为一致
- **Git 扫描代码复用**：封装 Git 命令执行与 .java 文件路径解析逻辑，最近修改与贮存区扫描共享实现，日志输出更加清晰

---

## [1.1.0] - 2026-02-28

### 新增

- **扫描最近 Git 修改**：在 JavaClassScanner 中新增「扫描最近git修改」按钮，按配置的天数、作者从 Git 历史中获取修改过的 Java 类
- **批量优化**：多选类后点击「开始优化」，批量调用灵码的优化功能（TriggerCosyOptimizeCodeGenerationAction）
- **智能优化策略**：
  - `@Data` 注解的类（VO/DTO 等）：整类发送一次优化
  - 普通类：遍历每个方法，逐个发起优化
- **Git 配置**：支持回溯天数 1-1000 天，支持按作者过滤（留空为当前用户）
- **配置持久化**：配置保存到 `~/.lingma-plus/lingma-config.json`，重启 IDE 后自动加载
- **便捷按钮**：JavaClassScanner 面板新增「配置」「设置」按钮，方便快速访问

### 改进

- 表格支持多选，按表格顺序逐个类处理
- 配置对话框增加 Git 相关设置项

---

## [1.0.2]

### 更新内容

- 优化用户体验
- 改进稳定性
- 调整部分细节

---

## [1.0.1] / [1.0.0]

- 初始版本
- 批量解释类中方法
- 自定义时间间隔配置
